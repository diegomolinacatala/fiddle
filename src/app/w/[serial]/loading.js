import { EsqueletoCaja } from "@/app/Esqueleto";

// Tras escanear: la ficha del cliente aparece en gris en el acto, no una pantalla en blanco.
export default function Cargando() {
  return <EsqueletoCaja />;
}
